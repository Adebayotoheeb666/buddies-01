// Seed database function - keeping for compatibility but not used
const seedDatabase = async () => {
  console.log("Seed function disabled - using live data only");
  return;
};

export default seedDatabase;
